# lib

- `openai.ts` – OpenAI client (uses user key from settings)
- `scorm/` – manifest builder, API wrapper, packager
- `storage.ts` – object storage for images, SCORM zips
- `crypto.ts` – encrypt/decrypt API keys

To be implemented in Phase 1–4.
